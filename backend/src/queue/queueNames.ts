export const JOB_LIST_KEY = "fraud:jobs";

export const sessionKey = (sessionId: string) => `session:${sessionId}`;
export const resultKey = (sessionId: string) => `result:${sessionId}`;
export const resultChannel = (sessionId: string) => `result:${sessionId}`;
